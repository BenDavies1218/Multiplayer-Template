name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build-binaries:
    name: Build Release Binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: ''
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: ''
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: .exe
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: ''
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            suffix: ''

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-release-${{ hashFiles('**/Cargo.lock') }}

      - name: Install cross-compilation tools (ARM Linux)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu
          echo "CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER=aarch64-linux-gnu-gcc" >> $GITHUB_ENV

      - name: Build server
        run: cargo build --release -p server --target ${{ matrix.target }}

      - name: Build native client
        run: cargo build --release -p native --target ${{ matrix.target }}

      - name: Build world-viewer
        run: cargo build --release -p world-viewer --target ${{ matrix.target }}

      - name: Package binaries (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          mkdir -p release
          cp target/${{ matrix.target }}/release/server release/server-${{ matrix.target }}${{ matrix.suffix }}
          cp target/${{ matrix.target }}/release/native release/native-client-${{ matrix.target }}${{ matrix.suffix }}
          cp target/${{ matrix.target }}/release/world-viewer release/world-viewer-${{ matrix.target }}${{ matrix.suffix }}
          tar -czf multiplayer-template-${{ matrix.target }}.tar.gz -C release .

      - name: Package binaries (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          mkdir release
          cp target/${{ matrix.target }}/release/server${{ matrix.suffix }} release/server-${{ matrix.target }}${{ matrix.suffix }}
          cp target/${{ matrix.target }}/release/native${{ matrix.suffix }} release/native-client-${{ matrix.target }}${{ matrix.suffix }}
          cp target/${{ matrix.target }}/release/world-viewer${{ matrix.suffix }} release/world-viewer-${{ matrix.target }}${{ matrix.suffix }}
          Compress-Archive -Path release/* -DestinationPath multiplayer-template-${{ matrix.target }}.zip

      - name: Upload artifacts (Unix)
        if: matrix.os != 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          name: binaries-${{ matrix.target }}
          path: multiplayer-template-${{ matrix.target }}.tar.gz

      - name: Upload artifacts (Windows)
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          name: binaries-${{ matrix.target }}
          path: multiplayer-template-${{ matrix.target }}.zip

  build-web:
    name: Build Web Client
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install Trunk
        run: cargo install --locked trunk

      - name: Build web client
        working-directory: apps/web
        run: trunk build --release

      - name: Package web client
        run: |
          cd apps/web
          tar -czf ../../multiplayer-template-web.tar.gz dist/

      - name: Upload web artifact
        uses: actions/upload-artifact@v7
        with:
          name: web-client
          path: multiplayer-template-web.tar.gz

  create-release:
    name: Create GitHub Release
    needs: [build-binaries, build-web]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*
          generate_release_notes: true
          draft: false
          prerelease: false
