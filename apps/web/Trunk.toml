[build]
target = "index.html"
# Ensure getrandom uses the correct backend for WASM
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]

[watch]
# Watch for changes in src and assets
ignore = ["dist/"]
